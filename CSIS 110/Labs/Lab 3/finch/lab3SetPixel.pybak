# Matt Pigliavento, Matt Curtin 

# lab3SetPixel.py
# Complete what needs to go between each set of parentheses; everything else is
# complete and correct!

# name the inputs to setPixel inside the next set of parentheses
def setPixel (myPic, x, y, myColor):

# complete these sets of parentheses, being careful to use only names you
# created in the line above
  p = getPixel(myPic, x, y)
  setColor(p, myColor)
  explore (myPic)
  